The maven-wrapper.jar is not included in this repo (binary file).
Run: mvn wrapper:wrapper inside the backend/ directory to generate it,
OR let the Dockerfile download it automatically via the Maven Wrapper protocol.
